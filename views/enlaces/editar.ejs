<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Enlace</title>
</head>
<body>
    <h1> Editar Enlace </h1>
    <% if (error && error != "") { %>
        <p style="color:red"><%= error %></p>
    <% } %>
    <% if (ok && ok != ""){ %>
        <p style="color:chartreuse"> <%= ok %> </p>
    <%} %>
    <form method="POST" action="/enlaces/<%= id %>/editar">
        <input type="text" name="titulo" value="<%= form.titulo %>">
        <input type="text" name="url" value="<%= form.url %>">
        <textarea name="descripcion"><%= form.descripcion %></textarea>
        <button type="submit"> Guardar </button>
        <a href="/temas/<%= temaId %>"> Cancelar </a>
    </form>
</body>
</html>